#
# application.yml — главный конфигурационный файл Spring Boot.

Что нужно сделать:
  - Указать настройки подключения к базе данных.
  - Указать порт приложения.
  - Настроить параметры безопасности и JWT.
  - Включить Swagger UI.

server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bankcards
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update # validate
    show-sql: true
  liquibase:
    change-log: classpath:db/migration/db.changelog-master.yaml

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

app:
  jwt:
    access-token-secret: ${JWT_ACCESS_SECRET:mySecretAccessKeyForJWT123!}
    refresh-token-secret: ${JWT_REFRESH_SECRET:mySecretRefreshKeyForJWT789!}
    access-token-expiration: 10m
    refresh-token-expiration: 1h
  card-encryption:
    key: ${CARD_ENCRYPTION_KEY:trainingSecretKey}